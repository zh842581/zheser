# 2026-08-07 工作日志

## 阅读《公众号自媒体自动化热点追踪与内容生成方案.docx》
- 用户桌面文档，约 7500 字，规划"中年人浑身不得劲"公众号自媒体的自动化运营。
- 4 条路径：手动方案 / Agent自动化架构 / 零代码本地脚本(hotbot.py + tophub API + pyinstaller + Win任务计划) / Coze可视化平台。
- 文档误判 WorkBuddy 为"仅对话机器人"，实际 WorkBuddy 可写脚本、跑代码、建定时自动化、生成配图。
- 关键依赖：tophub.today API Key；用户本地大模型地址（疑似 http://127.0.0.1:1234/v1/chat/completions，需 OpenAI 兼容接口）。
- 已向用户说明可落地能力，并给出 A/B/C 三选项待确认下一步。

## 选 A：编写 hotbot.py 并 dry-run
- 在 workspace 生成 hotbot.py（支持 HOTBOT_MOCK=1 模拟模式：用内置模拟热点数据，不消耗 tophub Key、不访问本地模型）。
- 用 `HOTBOT_MOCK=1 python hotbot.py` 成功跑通，产出 output/report_20260807_2201.txt：四平台共筛出 7 条相关热点 + MOCK 示例文章。
- 真实模式需用户提供：TOPHUB_API_KEY、本地模型地址(默认 127.0.0.1:1234/v1/chat/completions)。
- 后续：B=真实跑通(需Key)；C=建每日7:30定时自动化。

## 用 TrendRadar 替代 tophub（用户无 API Key）
- 用户无 TOPHUB_API_KEY，要求用 TrendRadar 追踪各平台热点。官方 GitHub 仓库在本环境不可达(000)，故按 TrendRadar 思路自建等价本地模块。
- 新建 trendradar.py：标准库实现，多平台公开热榜接口 + 解析器 + 关键词漏斗，无 API Key，单平台失败自动跳过。
- 平台可达性（本沙箱实测）：头条✅(50条, JSON data[].Title/Url/HotValue)、抖音✅(48条, data.word_list[].word)、百度✅(51条, 嵌套 data.cards[].content[].content[].word)；微博403/知乎401/B站403 需 cookie（用户本机有登录态可通）；快手需 graphql cookie 暂未实现。
- 改写 hotbot.py：数据源可切 trendradar(默认)/tophub/mock；HOTBOT_LLM_MOCK=1 模拟文章。已用 trendradar+llm_mock 真实跑通，产出 output/report_20260807_2215.txt(149条真实热榜→关键词0命中→追踪简报)。
- 关键结论：GitHub 在本沙箱不可达，但中文平台接口(头条/抖音/百度)可达，TrendRadar 方案成立。

## 放宽关键词 + 接入真实本地模型（22:20 后）
- 用户要求把选题从"浑身不得劲"放宽到健康管理大类（身体不舒服/缓解疲劳/放松心情/养生/睡眠/情绪/体检/亚健康…）。
- 已更新 hotbot.py + trendradar.py 的 KEYWORDS 为健康管理大类（~41 词，子串匹配）。
- 用户本地模型 http://127.0.0.1:1234（LM Studio，OpenAI 兼容）。实测可达：GET /v1/models 列出 qwen3vl-8b-instruct、qwen3.5-4b、gemma-4 系列等；POST /v1/chat/completions 中文正常。call_llm 用 model="local-model"（路由到当前加载模型）。
- 坑：沙箱 pip 装 requests 时 pypi.org 超时。已把 hotbot 的 LLM 与 tophub 调用全改为标准库 urllib，实现零外部依赖。
- 真实跑通：HOTBOT_SOURCE=trendradar 抓 150 条 → 放宽关键词命中头条《老中医：立秋后养心是关键》/百度《全国睡眠舒适度地图》 → 本地模型生成完整爆文（3标题+正文+建议+互动），产出 output/report_20260807_2228.txt。整条流水线在用户本机可用、无 API Key、无 pip 依赖。

## 双方案（local手动 + cloud定时）+ 微信推送（22:40-22:50）
- 用户要两方案：①本地模型(local，手动开机点击运行) ②云端开源模型(cloud，定时自动化)，两者都推微信。
- 关键澄清：调网上模型**不需要**自建云空间——用 API 服务商(硅基流动/DeepSeek等)只需 Key；微信推送用 Server酱/PushPlus/WxPusher 等 SaaS 也只需注册。
- 新增 wechat_push.py：标准库，支持 serverchan/pushplus/wxpusher，读 config.json.wechat 或环境变量，未配置则跳过。
- 重构 hotbot.py：model_mode=local|cloud（local 默认 127.0.0.1:1234；cloud 读 config.cloud 的 url/key/model），接入微信推送；保留 trendradar/tophub/mock 数据源与健康管理关键词。
- 新增 config.json（local/cloud/wechat/keywords 配置位）与 run_local.bat（双击以 local 模式运行）。
- 已建 recurring 自动化 automation-1786113997673：每天 7:30，HOTBOT_MODEL_MODE=cloud 跑 hotbot 并推微信（PC 不开机也能跑，前提是自动化在云端执行）。
- 待用户提供：云端模型 API Key + 选择的微信推送服务 token（填 config.json 即可）。

## 微信推送改用飞书/钉钉（22:55 后）
- 用户反馈 Server酱 Turbo 免费版微信通知只显示标题、不显示内容。已扩展 wechat_push.py 支持免费且完整显示正文的方案。
- 新增 feishu（飞书群机器人 webhook，interactive 卡片 markdown）与 dingtalk（钉钉群机器人 webhook，markdown）两种模式，均支持可选加签(secret)。config.json.wechat 增 webhook / secret 字段。
- 免费且显示完整正文的推荐：飞书/钉钉群机器人（建群拉自己+机器人即可，零费用、完整 markdown）；PushPlus（推微信、完整 markdown，免费）。

## 云端大模型免费 API 调研（23:02 后，用户想先测试）
- 用户问云端模型是否有免费的。结论：有，且都 OpenAI 兼容，可直接套进 hotbot 的 cloud 模式。
- 中文最强+真免费：
  - 硅基流动 SiliconFlow（api.siliconflow.cn/v1）：9B以下模型永久免费不限量，如 Qwen/Qwen2.5-7B-Instruct、Qwen/Qwen3-8B、THUDM/glm-4-9b-chat、deepseek-ai/DeepSeek-R1-Distill-Qwen-7B；需实名(微信/支付宝人脸)。新用户送约2000万token+14元。
  - 智谱 GLM（open.bigmodel.cn/api/paas/v4）：GLM-4-Flash / GLM-4.7-Flash 永久免费，128K/200K 上下文，中文强。
  - 阿里云百炼（dashscope.aliyuncs.com/compatible-mode/v1）：每模型100万token/3个月免费，Qwen系。
- 免手机验证、注册最省事：OpenRouter（openrouter.ai/api/v1，:free 模型200次/天免费，如 openrouter/free 或 Qwen/...:free）、Groq（api.groq.com/openai/v1，qwen/qwen3-32b 免费、极快）。
- 新增 test_cloud.py：读 config.cloud，发一条健康类测试 prompt 验证云端 Key 是否可用（未填Key优雅提示）。

## 填入硅基流动 Key 并端到端验证（23:06 后）
- 用户提供了硅基流动 API Key（sk-gz...，已写入 config.json.cloud.api_key，模型改为 Qwen/Qwen2.5-7B-Instruct 免费中文强模型）。
- test_cloud.py 首次误把真实 Key 前缀 sk- 当占位符，已修正占位符判断（不再含 "sk-"）。
- test_cloud.py 实测成功：云端模型返回正确的中文健康回答。
- HOTBOT_MODEL_MODE=cloud 跑通整条流水线：trendradar 抓 149 条（头条50/抖音48/百度51，微博403/知乎401/B站403 沙箱无cookie）→ 云端 Qwen 模型基于头条《老中医：立秋后养心是关键》生成完整爆文，产出 output/report_20260807_2308.txt。方案B（云端+定时）已端到端验证可用。
- 待办：方案B仍缺微信推送配置（feishu/dingtalk webhook 未给）。方案A(本地双击run_local.bat)已可用。

## 改用 Qwen/Qwen3-VL-8B-Instruct + 自定义风格（23:16 后）
- 用户想用硅基流动 Qwen3VL-8B-Instruct。注意准确模型ID是 `Qwen/Qwen3-VL-8B-Instruct`（带连字符，写错返回 Model does not exist）。8B、无 Pro/ 前缀，符合"9B以下永久免费"政策→免费。
- 已在 config.json 设置：cloud.model 改该模型；新增顶层 `system_prompt`（禁清单体/禁鸡汤句/结尾不准升华总结给建议、用自嘲骂街句收尾如"算求了""不说了外卖到了"）；新增顶层 `gen_params`={temperature:1.2,top_p:0.95,top_k:60,repetition_penalty:1.1(即用户说的repeat penalty),presence_penalty:1.8,min_p:0,max_tokens:2000}。
- 硅基流动参数名是 `repetition_penalty`（非 repeat_penalty），hotbot/test_cloud 均已注入。
- hotbot.py：call_llm 改用 SYSTEM_PROMPT + GEN_PARAMS（从config读，env可覆盖）；ARTICLE_PROMPT 移除"给建议+引导互动"要求，结尾交由system_prompt管控。test_cloud.py 同步用 system_prompt+gen_params。
- 实测：`test_cloud.py` 与 `HOTBOT_MODEL_MODE=cloud hotbot.py` 均成功，文章结尾为"算求了！！"符合风格要求，产物 output/report_20260807_2322.txt。
- 注：models_list.json 为调试临时文件，已删除。

## 复位 system_prompt 与模型参数（23:30 后）
- 用户要求：去掉 system prompt 内容、模型参数复位默认（以后再慢慢改）。
- config.json：system_prompt 改为 ""（空）；gen_params 复位为标准基线 {temperature:0.8, top_p:1.0, top_k:40, repetition_penalty:1.05, presence_penalty:0.0, min_p:0.0, max_tokens:2000}。
- hotbot.py：SYSTEM_PROMPT 兜底改为 ""（不再注入"健康自媒体助手"人设）；call_llm 仅当 SYSTEM_PROMPT 非空才加 system 消息；ARTICLE_PROMPT 移除"结尾按系统指令处理"那行（已无 system prompt 约束）。
- test_cloud.py：同步，system_prompt 空则不注入 system 消息。
- 实测：test_cloud.py 默认参数 + 空 system prompt 调用云端 Qwen/Qwen3-VL-8B-Instruct 成功，返回标准顺从回答（风格恢复正常）。
- 用户问 PushPlus 是否免费及注册方式：是，免费 200条/天、微信服务通知显示完整 markdown 正文；但 2024-08-01 起需微信实名认证（认证可能收少量费用）才能调用发送接口。注册：pushplus.plus 微信扫码登录→左侧"发送消息"复制 Token。

## 钉钉群机器人推送配置（00:12 后，跨日 08-08）
- 用户选择钉钉群机器人作为推送渠道，并提供加签 SECRET（SEC8aa...，已写入 config.json.wechat.secret）。
- config.json.wechat.mode 设为 "dingtalk"；webhook 待用户提供（创建机器人时带 access_token 的完整 URL）。
- 用户要求"两个方案都要"：方案A(local 手动双击 run_local.bat) 与 方案B(cloud 每日7:30自动化) 共用同一 wechat_push 配置，填好 webhook 后即同时生效。
- wechat_push.py 钉钉逻辑已核对：毫秒时间戳 + HMAC-SHA256 加签，拼到 webhook 的 &timestamp=&sign=，正确。
- 待办：用户提供钉钉 webhook → 跑 `python wechat_push.py` 真实推送测试 → 两方案闭环。
