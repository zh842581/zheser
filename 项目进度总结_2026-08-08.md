# 公众号自动化热点追踪与内容生成 —— 项目进度总结

> 生成日期：2026-08-08（凌晨）｜ 目的：下次打开即可接上进度
> 项目目录：`C:\Users\zhese\WorkBuddy\2026-08-07-21-54-30`

---

## 一、最终目标（内容生产链路）

```
热点采集 → 关键词筛选 → 大模型创作 → 推送审核 → 你人工发布
(多平台热榜+  (健康养生类)  (公众号爆文)  (钉钉/微信)  (公众号/贴图/头条号)
 TrendRadar简报)
```

- **你定义的完整目的**：用「热点简报推送」内容 + 各平台「爆文」→ 自动分析出符合关键词（健康养生）的文章/热点 → 大模型创作 → 推送 → 你再发布到 **公众号 / 贴图（图文平台，疑指小红书类）/ 头条号**。
- **推送定位**：钉钉/微信只是「你的审核入口」，**不是自动发到公众号**。公众号/头条号自动发布需认证服务号 + 官方 API，暂不做，你手动复制粘贴发布更稳、避坑。

---

## 二、今天定下的关键决策

| 事项 | 决定 |
|------|------|
| 采集源 | **双管齐下**：自研 `trendradar.py` 为主 + 真正 TrendRadar 简报作补充 |
| 部署 | **先测 GitHub Actions 连通性**，再定最终部署（云端全自动 / 本机定时 / 国内云） |
| 真正 TrendRadar 的角色 | 只做「采集 + 热点简报」；**不写公众号爆文**，成文必须由自研 `hotbot.py` 完成 |
| 推送渠道 | 钉钉（已通，加签模式）为主；Server酱/PushPlus/飞书/微信可选 |

---

## 三、已经实现的东西

1. **自研全链路 `hotbot.py`**（采集→筛选→创作→推送）
   - 数据源 `trendradar.py`：头条 / 抖音 / 百度 公开热榜，**无需任何 API Key**（本沙箱实测可达）。
   - 关键词：健康养生大类（约 41 词：不舒服/疲劳/睡眠/焦虑/养生/体检/中年…）。
   - 双模型：`local`（本机 `http://127.0.0.1:1234`）/ `cloud`（硅基流动 OpenAI 兼容）。
   - 推送：`wechat_push.py` 支持 钉钉(加签)/Server酱/PushPlus/飞书/微信。
   - **本次新增 `BRIEFING_FILE` 能力**：从外部简报（markdown 链接或纯文本）抽取候选热点，合并进选题池 → 双管齐下接口已就绪（已编译+功能测试通过）。
2. **真正 TrendRadar（官方 `sansan0/TrendRadar`）Docker 工作流** → `trendradar-real.yml` + `trendradar-real/frequency_words.txt`。
3. **GitHub Actions 云端版** → `daily_hotspot.yml`（cron `30 23 * * *` = 北京 07:30，cloud 模式 + 钉钉推送）。
4. **连通性诊断** → `connectivity-test.yml` + `.github/scripts/conn_test.py`（本次新增，见下）。

---

## 四、文件清单（项目根目录）

| 文件 | 作用 |
|------|------|
| `hotbot.py` | 主流程：采集→筛选→大模型成文→推送（含 `BRIEFING_FILE` 吸收简报） |
| `trendradar.py` | 自研多平台热榜采集（头条/抖音/百度） |
| `wechat_push.py` | 推送模块（钉钉/Server酱/PushPlus/飞书/微信） |
| `config.json` | **真实配置（含密钥，已被 .gitignore 忽略，勿提交）** |
| `config.example.json` | 脱敏配置模板（可提交） |
| `run_local.bat` | 本机双击以 local 模式运行 |
| `test_cloud.py` | 云端模型单次连通测试 |
| `.github/workflows/daily_hotspot.yml` | 云端定时版（7:30，cloud） |
| `.github/workflows/trendradar-real.yml` | 真正 TrendRadar Docker 版 |
| `.github/workflows/connectivity-test.yml` | **连通性诊断（本次新增）** |
| `.github/scripts/conn_test.py` | **诊断脚本：测 9 个端点可达性（本次新增）** |
| `trendradar-real/frequency_words.txt` | 健康管理关键词 + 屏蔽八卦词 |

---

## 五、Git 状态

- 本地已提交 3 个 commit（按时间）：
  - `84599c7` 基础：公众号健康管理热点追踪 + 云端自动出稿
  - `743dc49` 接入官方真正版 TrendRadar（Docker + 关键词 + 钉钉/硅基流动 AI）
  - `227e951` 本次：新增连通性诊断 + hotbot 支持吸收外部简报（双管齐下）
- **远程尚未添加**，需要你 push（见第六节）。
- 尚未跟踪：`run_local.bat`、`test_cloud.py`、`.workbuddy/`（均不提交）。

---

## 六、明天要做（下一步，按顺序）

1. **推到 GitHub**（之前一直没推）：
   ```bash
   git config user.email "你的真实@邮箱"      # 若还是占位先改掉
   git remote add origin https://github.com/<你的用户名>/hotspot-bot.git
   git push -u origin main
   ```
2. **跑连通性诊断**（**不需要任何密钥**，纯测网络）：
   GitHub 仓库 → **Actions** → 找到「连通性诊断 Connectivity Test」→ **Run workflow**（手动触发）。
   它会测：头条/抖音/百度/微博（国内采集源）、硅基流动（云端模型）、钉钉（推送）、DockerHub/GitHub/newsnow（基础设施），结果是一张 ✅/❌ 表格贴在运行摘要里。
3. **把诊断结果（✅/❌ 表格）发给我**，或告诉我哪几个 ✅、哪几个 ❌。
4. **据结果定最终部署 + 接入 TrendRadar 简报**：
   - 若国内采集源 ✅ → GitHub Actions 云端全自动可行，我用组合工作流把真正 TrendRadar 的简报通过 `BRIEFING_FILE` 接入 `hotbot.py`，完成双管齐下闭环。
   - 若国内采集源 ❌（美国服务器抓不到头条/抖音/百度）→ 退回本机定时（电脑开着最稳）或上国内云/CI。

---

## 七、已知坑 / 注意事项

- ⚠️ 真正 TrendRadar 的 AI = **热点简报**，不是公众号爆文；必须保留自研 `hotbot.py` 成文模块。
- ⚠️ 真正 TrendRadar 的数据源 newsnow 在 **Actions 美国服务器**访问国内平台（微博/知乎/B站）可能不稳。
- ⚠️ 钉钉机器人现在是「**加签**」模式；真正 TrendRadar 大概率不支持加签，走 TrendRadar 那条线前需去钉钉改成「**自定义关键词**」。
- GitHub 定时任务可能延迟；仓库 **60 天无活动会自动停用** scheduled workflow。
- 微博/知乎/B站 在本沙箱返回 403（需登录 cookie）；你本机有登录态通常可通。
- 沙箱 `pip install` 超时，所有脚本一律用**标准库（urllib）**，不依赖 requests。

---

## 八、密钥 / 配置速查（勿外泄）

- **硅基流动 API Key**：`config.json → cloud.api_key`（免费档：Qwen / Qwen3-VL 等 9B 以下永久免费）。
- **钉钉机器人 webhook + secret**：`config.json → wechat.webhook / wechat.secret`（加签模式）。
- **GitHub Secrets**（仅跑 `daily_hotspot` / `trendradar-real` 时需要；连通性诊断不需要）：
  `SILICONFLOW_API_KEY` / `DINGTALK_WEBHOOK` / `DINGTALK_SECRET`。

---

## 九、一句话回顾

> 我们要的是「自动抓热点→筛选健康选题→大模型写公众号爆文→推给你审核→你手动发」。自研 `hotbot.py` 已经能跑通这条线；真正 TrendRadar 只是更全的采集补充。眼下唯一要做的实测是：**GitHub Actions 美国服务器到底能不能抓到国内热榜**——跑一次连通性诊断就知道了。

晚安，明天见 👋
